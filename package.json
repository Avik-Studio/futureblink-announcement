{
  "name": "futureblink-announcement-app",
  "version": "1.0.0",
  "private": true,
  "description": "Shopify embedded app: save an announcement from the Admin, store it in MongoDB, sync it to a Shop metafield, and show it on every storefront page via a Theme App Extension.",
  "license": "MIT",
  "scripts": {
    "dev": "shopify app dev",
    "build": "shopify app build",
    "deploy": "shopify app deploy",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "config:link": "shopify app config link",
    "config:use": "shopify app config use",
    "start": "cd web && npm run serve"
  },
  "devDependencies": {
    "@shopify/cli": "^4.3.0"
  }
}
